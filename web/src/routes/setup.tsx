import { Screen } from '../components/Screen';

export default function Setup() {
  return (
    <Screen kicker="Register" title="Your household">
      <p>Address pin, building type, floor level, household size, dependencies, vehicle, language. Under 60 seconds, every field skippable. Placeholder — built next.</p>
    </Screen>
  );
}
