import { Screen } from '../components/Screen';

export default function Shelters() {
  return (
    <Screen kicker="During" title="Shelters">
      <p>Shelters near this address with live capacity, and a route that avoids what is rising. Placeholder — built next.</p>
    </Screen>
  );
}
