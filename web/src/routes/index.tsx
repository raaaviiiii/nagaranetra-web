import { Screen } from '../components/Screen';

export default function Dashboard() {
  return (
    <Screen kicker="Your household" title="Dashboard">
      <p>The hazards at this address right now, each against this household&rsquo;s own threshold. Placeholder — built next.</p>
    </Screen>
  );
}
