import { Screen } from '../components/Screen';

export default function Help() {
  return (
    <Screen kicker="Emergency" title="Get help">
      <p>One tap, no account, works offline. Voice in Malayalam or icons — completable without reading. Placeholder — built next.</p>
    </Screen>
  );
}
